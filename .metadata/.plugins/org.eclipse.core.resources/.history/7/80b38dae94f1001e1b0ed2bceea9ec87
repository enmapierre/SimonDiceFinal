package clases;

public class Main {
	/**
	 * Engine eng=new Engine(); crea una nueva instancia de la clase Engine y le
	 * asigna a la variable eng. eng.start(); llama al método start.
	 * 
	 * @param args
	 * @throws IOException
	 */
	public static void main(String[] args) throws IOException {
		Engine eng = new Engine();
		eng.start();

	}
}
